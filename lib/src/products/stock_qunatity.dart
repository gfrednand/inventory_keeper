import 'package:flutter/material.dart';

class StockQuantity extends StatelessWidget {
  const StockQuantity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
